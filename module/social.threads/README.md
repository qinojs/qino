# social.threads

Configure a Threads user access token carrying `threads_basic` and `threads_content_publish`. The
authenticated account is the module's single target.

The module publishes plain text through Threads' automatic text publishing flow. OAuth connection,
token refresh, media, replies and synchronization are deliberately not part of the minimal version.
