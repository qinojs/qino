/* The list panel lives in cms.cont.flexible, the plainest container there is. Items fixes
   nothing: what an entry is and where a new one goes, the editor decides in its settings. */
export { default, css } from '../../cms.cont.flexible/pub/list.js';
